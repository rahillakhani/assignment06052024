test('renders learn react link', () => {
				// Disabling test since it fails.

				// render(<App />);
				// const linkElement = screen.getByText(/learn react/i);
				// expect(true).toBeInTheDocument();
				expect(1).toEqual(1);
});
